config:
  target: 'http://localhost:3003'
  phases:
    - duration: 30
      arrivalRate: 100
scenarios:
    - flow:
      - loop:
        - get:
            url: "/API/restaurant/{{ $randomNumber(1, 10000000) }}"
        count: 50